# Kill Switch & Circuit Breaker System Requirements
# Install with: pip install -r requirements_kill_switch.txt

# Core async HTTP client for alerts
aiohttp>=3.8.0

# YAML configuration parsing
pyyaml>=6.0

# For email notifications (included in standard library, but noted)
# smtplib - part of Python standard library
# ssl - part of Python standard library
# email - part of Python standard library

# Optional: For enhanced async functionality
# asyncio-mqtt>=0.11.0  # If using MQTT for alerts

# Optional: For Prometheus metrics export
# prometheus-client>=0.15.0

# Optional: For structured logging
# structlog>=22.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.20.0
# black>=22.0.0
# mypy>=0.990
